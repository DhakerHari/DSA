#include <iostream>
using namespace std;
int main()
{

    // Q1 Write a C++ program to find the largest element of a given array of integers.
    // int a[5] = {1, 2, 3, 4, 5};
    // int max = a[0]; int i;
    // for (i = 0; i < 5; i++)
    // {
    //     if (max1 > a[i])
    //     {
    //         max = a[i + 1];
    //     }
    // }
    // cout << max;

    // Q2. Write a C++ program to find the largest three elements in an array.
    // int arr[5] = {1, 2, 3, 4, 5};
    // int max1 ,max2,max3;
    // max1 = max2 = max3 = arr[0];
    // int i;
    // for (i = 0; i < 5; i++)
    // {
    //    if (arr[i] > max1)
    //     {
    //         max3 = max2; // Updating third largest element
    //         max2 = max1; // Updating second largest element
    //         max1 = arr[i]; // Updating first largest element
    //     }
    //     else if (arr[i] > max2)
    //     {
    //         max3 = max2; // Updating third largest element
    //         max2 = arr[i]; // Updating second largest element
    //     }
    //     else if (arr[i] > max3)
    //     {
    //         max3 = arr[i]; // Updating third largest element
    //     }
    // }
    // cout << max1<<"\n"<<max2<<"\n"<<max3;

// Q3. Write a C++ program to find the second largest element in an array of integers.
    // int arr[5] = {1, 2, 3, 4, 5};
    // int max, smax;
    // max = smax = arr[0];
    // for (int i = 0; i < 5; i++)
    // {
    //     if (max < arr[i])
    //     {
    //         smax = max;
    //         max = arr[i];
    //     }
    //     else if ((smax<arr[i])&&(max!=arr[i]))
    //     {
    //         smax = arr[i];
    //     }
    // }
    // cout << "first largest : " << max;
    // cout << "\n second largests : " << smax;

// Q
    return 0;
}